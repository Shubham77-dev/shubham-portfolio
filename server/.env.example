PORT=5000
MONGODB_URI=mongodb+srv://patidarshubham443_db_user:Shubham1234@cluster0.tchdcls.mongodb.net/?appName=Cluster0
JWT_SECRET=shubham_secure_jwt_key_2026
JWT_EXPIRES_IN=7d

# Optional: only users registered with the correct invite token can become `admin`.
# Set this in your real .env file. For local dev you can set it to any string.

ADMIN_INVITE_TOKEN=make_me_admin_123

# Optional: frontend origin for CORS
CLIENT_ORIGIN=http://localhost:5173
